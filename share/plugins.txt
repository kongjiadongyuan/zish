# Managed by zish; edits may be replaced.
# Latest from network on each install (no commit pins).

zsh-users/zsh-completions kind:fpath path:src
# ez-compinit omitted: one cached compinit in config.zsh
Aloxaf/fzf-tab

zsh-users/zsh-autosuggestions
zsh-users/zsh-history-substring-search
# Clone only; sourced lazily from config.zsh when abbreviations exist or `abbr` runs.
olets/zsh-abbr kind:clone

# Syntax highlighting must be loaded last.
zsh-users/zsh-syntax-highlighting
